
<%= form_for([@learningstyle.user, @learningstyle]) do |form| %>

  

  <%= form.hidden_field :user_id,value: current_user.id %>
   
  
    <%= form.submit 'Start',class: "btn btn-primary btn-lg text-center" %>
   

<% end %>


