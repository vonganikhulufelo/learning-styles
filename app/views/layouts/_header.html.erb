    <div class="bs-example">
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <%= link_to "Learning Style",root_path ,class: "navbar-brand" %>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                  <span class="navbar-toggler-icon"></span>
                </button>
            

        <div class="collapse navbar-collapse" id="navbarColor02">
            <ul class="navbar-nav ml-auto">  
              <li class="nav-item"><%= link_to '<i class="fas fa-home"></i> Home'.html_safe, root_path,class: "nav-link" %></li>
              <li class="nav-item"><%= link_to '<i class="fas fa-plus"></i> New organization'.html_safe, new_user_organization_path(current_user.id),class: "nav-link" %></li>
              <li class="nav-item"><%= link_to 'Questionnaire <i class="fas fa-question"></i>'.html_safe, new_user_learningstyle_url(current_user.id),class: "nav-link" %></li>
              <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false"><span class="user-name">Logged as <%= current_user.name %></span> </a>
              <div class="dropdown-menu" x-placement="bottom-start">
                <%= link_to '<i class="fa fa-user"></i> Profile'.html_safe,"",class: "dropdown-item"%>
                <%= link_to '<i class="fa fa-cog"></i> Settings'.html_safe, edit_user_path(current_user.id),class: "dropdown-item"%>
                <div class="dropdown-divider"></div>
                <%= link_to  '<i class="fas fa-sign-out-alt"></i> Sign out'.html_safe, logout_path,class: "dropdown-item" %>
              </div>
            </li>
            </ul>
    </nav>
  </div>   
     <%# if (controller.controller_name == "users" && controller.action_name == "new") || (controller.controller_name == "users" && controller.action_name == "index") %>
 
   <%# else %> 

  <div class="container"> 
      <%# end %>